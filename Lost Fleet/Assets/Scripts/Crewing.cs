using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Crewing : MonoBehaviour
{
    public const int MAX_CREW = 10;
    public static int crew  = MAX_CREW;
    public static float Readiness { get; private set; } = 1f;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
